{{#if link}}
  <a href="{{link.url}}" class="btn{{#each modifier}} btn--{{.}}{{/each}}{{#if isLoading}} is-loading{{/if}}{{#if isActive}} is-active{{/if}}{{#each class}} {{.}}{{/each}}"{{#if isDisabled}} disabled{{/if}}>
{{else}}
  <button type="button" class="btn{{#each modifier}} btn--{{.}}{{/each}}{{#if isLoading}} is-loading{{/if}}{{#if isActive}} is-active{{/if}}{{#each class}} {{.}}{{/each}}"{{#if isDisabled}} disabled{{/if}}>
{{/if}}
{{#if isLoading}}
  <span class="btn_content">
{{/if}}
{{#if icon}}
  {{render '@icon' icon merge=true}}
{{else}}
  {{text}}
{{/if}}
{{#if isLoading}}
  </span>
  <div class="btn_progress">
    {{render '@progress--circular-small'}}
  </div>
{{/if}}
{{#if link}}
  </a>
{{else}}
  </button>
{{/if}}
<!--TODO: Add variants to include focus states -->
<!--TODO: Consolidate the button templates -->
<!--TODO: Consider better name for class for setting new component classes -->